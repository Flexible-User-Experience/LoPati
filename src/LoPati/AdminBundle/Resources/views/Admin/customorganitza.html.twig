{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <div>
        {% if object.organitza %}
            {{ object.organitza | raw }}
        {% else %}
            ---
        {% endif %}
    </div>
{% endblock %}