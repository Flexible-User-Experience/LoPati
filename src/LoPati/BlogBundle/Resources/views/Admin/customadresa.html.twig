{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <div>
        {% if object.adresa %}
            {{ object.adresa | raw }}
        {% else %}
            ---
        {% endif %}
    </div>
{% endblock %}