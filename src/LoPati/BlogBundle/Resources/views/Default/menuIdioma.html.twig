<p>
    {% if app.request.locale == 'ca' %}
        <a href="{{ path('portada', {_locale : 'ca'}) }}"><strong>català</strong></a>
    {% else %}
        <a href="{{ path('portada', {_locale : 'ca'}) }}">català</a>
    {% endif %}
    |
    {% if app.request.locale == 'es' %}
        <a href="{{ path('portada', {_locale : 'es'}) }}"><strong>español</strong></a>
    {% else %}
        <a href="{{ path('portada', {_locale : 'es'}) }}">español</a>
    {% endif %}
</p>
